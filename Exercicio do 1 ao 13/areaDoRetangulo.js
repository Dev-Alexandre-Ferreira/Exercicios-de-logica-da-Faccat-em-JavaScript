alert("Área do retangulo");

let baseRetangulo = parseInt(prompt("Digite o tamanho da base do retangulo: "));

let alturaRetangulo = parseInt(
  prompt("Digite o tamanho da altura do retangulo: ")
);

let areaDoRetangulo = baseRetangulo * alturaRetangulo;

alert(`A área total do retangulo é de ${areaDoRetangulo} m²`);
